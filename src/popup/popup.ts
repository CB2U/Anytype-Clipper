// Popup UI entry point
console.log('Anytype Clipper popup loaded');

// DOM ready handler
document.addEventListener('DOMContentLoaded', () => {
  console.log('Popup DOM ready');
  // Will be extended with Space/Type selectors in Epic 7.0
});
